# Reich's score is copyrighted; keep MusicXML out of the public repo.
*.xml

# Source-WAV cache for build_samples.sh (large, rebuildable on demand).
# The chromatic mp3 banks under assets/audio/ ARE tracked so the deploy
# workflow can ship them — only the multi-megabyte source WAVs are
# excluded.
tools/.cache/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
